name: Unit Test

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
jobs:
  install:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup
        run: sh ./run.sh
      - name: Setup2
        run: sh ./run.sh
  approve:
    runs-on: macos-latest
    needs: "install"
    if: ${{ github.event_name == 'pull_request' }}
    steps:
      - uses: actions/checkout@v2      
      - name: approve
        run: gh pr review ${{ github.event.number }} --approve --body "LGTM 🙆‍♂️"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
