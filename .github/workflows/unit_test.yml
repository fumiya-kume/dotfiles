name: Unit Test

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
jobs:
  install:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup
        run: sh ./run.sh
      - name: Setup2
        run: sh ./run.sh
      - uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs')
            const homedir = require('os').homedir() + "/";
            const outputs = [[{data: 'path', header:true, colspan: '2'}]]
            const check = async (path) => {
              outputs.push([path, (await fs.existsSync(homedir + path))?"✅":"❌"])
            }
            
            await check(".gitconfig")
            await check(".config/fish/config.fish")
            await check(".config/starship.toml")
            await check(".config/fish")
            await check(".config/iterm")
            await check(".config/karabiner")
            await check(".Brewfile")
            await check(".gitconfig.remote")
            await check(".gitmodules")
            await check(".tigrc")
            await check(".gnupg")
            await check(".config")
            
            await core.summary
              .addTable(outputs)
              .write()
  approve:
    runs-on: macos-latest
    needs: "install"
    if: ${{ github.event_name == 'pull_request' }}
    steps:
      - uses: actions/checkout@v2      
      - name: approve
        run: gh pr review ${{ github.event.number }} --approve --body "LGTM 🙆‍♂️"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
