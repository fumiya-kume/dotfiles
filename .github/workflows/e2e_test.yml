name: E2E_Test

on:
  push:
    branches: [master]

jobs:
  E2E_Test:
    runs-on: macos-latest
    steps:
      - name: itest
        run: /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/fumiya-kume/dotfiles/master/bootstrap.sh)"
  approve:
    runs-on: ubuntu-latest
    needs: [E2E_Test]
    if: ${{ success() }}
    env:
      GITHUB_TOKEN: ${{ secrets.KUU_TOKEN }}
    steps:
      - uses: actions/checkout@v1
      - name: approve
        run: gh pr review ${{ github.event.number }} --approve --body "LGTM 🙆‍♂️"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
