name: E2E_Test

on:
  push:
    branches: [master]

jobs:
  E2E_Test:
    runs-on: macos-latest
    steps:
      - name: itest
        run: sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply fumiya-kume
